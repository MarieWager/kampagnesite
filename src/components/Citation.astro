---
import Button from "./Button.astro";
const {text, apo} = Astro.props;
---

<section>
    <img src=`${apo}` alt="Apostrophe" class="quotation"/>
    <p>{text}</p>
    <Button variant="light" goto="https://www.misbrugsportalen.dk" text="LÆS MERE" />
</section>

<style>
    section {
        position: relative;
        background-color: var(--dark-turquoise);
        border-radius: 5px;
        padding: 80px 0;
        margin: var(--with);
        margin-top: 190px;
        color: var(--light-grey);
    }
    .quotation {
        color: var(--turquoise);
        position: absolute;
        left: 44px;
        top: 25px;
        height: 4rem;
        width: 3rem;
    }
    p {
        padding: var(--with);
    }

    @media (min-width: 900px) {
        .quotation {
            top: 40px;
        }
        p {
            font-size: 1.7rem;
            line-height: 34px;
            padding-right: 120px;
        }
    }

    /* Button {
        right: 20px;
    } */
</style>

