---
import Button from "./Button.astro";
const {text} = Astro.props;
---

<section>
    <h1 class="quotation">"</h1>
    <p>{text}</p>
    <Button variant="light" text="LÆS MERE" />
</section>

<style>
    section {
        position: relative;
        background-color: var(--dark-turquoise);
        border-radius: 5px;
        padding: 100px 0;
    }
    .quotation {
        color: var(--turquoise);
        position: absolute;
        left: 10px;
    }
    p {
        padding: var(--with);
    }
</style>

